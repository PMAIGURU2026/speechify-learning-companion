# Supabase / PostgreSQL
# Get from Supabase Dashboard > Project Settings > Database
# If password has special chars (! @ # etc), URL-encode them: ! = %21, @ = %40
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres

# OpenAI (for quiz generation)
OPENAI_API_KEY=sk-your-openai-api-key

# JWT
# Generate a random string: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=[YOUR-JWT-SECRET]

# Server
PORT=3001

# Proxy for YouTube transcript (optional; required when YouTube blocks cloud IPs, e.g. on Render)
# Use PROXY_URL (recommended) or PROXY_HOST + PROXY_PORT + optional auth
# PROXY_URL=http://username:password@p.webshare.io:80
# Or: PROXY_HOST=p.webshare.io  PROXY_PORT=80  PROXY_USER=xxx  PROXY_PASS=xxx
